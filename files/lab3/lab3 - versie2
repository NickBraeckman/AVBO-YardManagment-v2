templist bevat: isSafe en !isSafe containers

while(!isSafe){

	if(LIFO.empty){
		cointainerToMove = searchYardForContainerToMove()

		if  (container == null) {
		    isSafe = true
		    return;
		}

		LIFO.put(containerToMove)

	} else {

		container = LIFO.get(0)
		do 1) - 3)
	}

}

1) kijk naar gewicht van foute container:
    1.1) gewicht is 1: is container de bovenste & hmax wordt niet overschreden?
        -> ja: plaats container er bovenop met zelfde lengte en groter gewicht
        -> nee: doe 1.2

    1.2) kijk naar hoogte in stack van die container -> waar kan foutecontainer tussengevoegd worden?
	    -> hoogte gevonden: steek container's vanaf die hoogte tot bovenste container in templist
		    * templist bevat: isSafe en !isSafe containers
		    * sorteer containers in templist op gewicht (kleinste gewicht vooraan)
	    -> hoogte niet gevonden: neem alle containers van stapeling en steek die in templist

2) plaats containers uit de templist:
	* plaats containers met compatibele lengte op zelfde plaats
	* plaats is tijdelijk niet veilig

3) bouw stapel terug op, op een veilige manier:
	3.1) kijk naar 1e container uit templist -> kan hij op de stapel (korte niet op lange)?
	        3.1.1) is de container langer dan de bovenste container van de huidige stapel (korte niet op lange)
		        -> ja: neem container uit templist en verplaats naar de huidige stapel
		        -> nee: isSafe?
			            -> ja: laat container staan
			            -> nee: voeg toe aan LIFO